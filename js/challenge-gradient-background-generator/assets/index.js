const inputs = document.querySelectorAll(".controls input");

/**
 * TODO: The two colour pickers above have been selected.
 * Add the appropriate JS event handler and bind it to the appropriate event listener (hint: change event).
 * So we can use the colour picker to select the colour to create the gradient background below
 *  */
